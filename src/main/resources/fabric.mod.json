{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "v${version}+${minecraft_version}",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${mod_author}\n\n"
  ],
  "contact": {
    "homepage": "https://modrinth.com/mod/${modrinth_slug}",
    "sources": "https://github.com/${github_repo}"
  },
  "license": "${license}",
  "icon": "${mod_id}.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "sylenthuntress.monstermash.MonsterMash"
    ],
    "fabric-datagen": [
      "sylenthuntress.monstermash.data.ModDataGenerator"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_version}",
    "minecraft": "${minecraft_version}",
    "java": ">=${java_version}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "monstermash.discord": "${discord_invite}"
      },
      "update_checker": true
    }
  }
}
